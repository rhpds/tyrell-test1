# Automation Manifest — DRAFT
# Generated during intake. Refined by automation procedures during development.

approach: ansible

infrastructure:
  type: ocp-cnv          # per-student topology on CNV
  topology: per-student
  cloud_provider: cnv
  cluster_type: sno
  ocp_version: "4.22"

operators: []            # No operators pre-installed — participant installs RHBK via OperatorHub

external_services:
  - registry.redhat.io
  - registry.access.redhat.com
